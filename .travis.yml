language: node_js
node_js:
  - 10
  - 12
sudo: false
before_install:
  - npm install -g codecov
cache:
  yarn: true
  directories:
    - node_modules
script:
  # Run test script
  - "yarn run lint"
  - "yarn test -- --maxWorkers=4"
after_script:
  - codecov
